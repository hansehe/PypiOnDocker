networks: 
    backend_network: false

volumes:
    portainer_data:
    postgres_data:
    pypi_data:

stacks:
    portainer: docker-compose.portainer.yml
    postgres_db: docker-compose.postgres.yml
    pypi: docker-compose.pypi.yml